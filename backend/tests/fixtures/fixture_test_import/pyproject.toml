[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "fixture-test-import"
version = "0.1.0"

[tool.pytest.ini_options]
asyncio_mode = "auto"
