import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "DevPilot — Multi-Agent Software Engineering Platform",
  description:
    "Autonomous multi-agent system that accepts GitHub issues and coordinates specialized AI agents to plan, implement, test, review, and document code changes.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
